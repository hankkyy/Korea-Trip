# 在璐上需求与产品红线

## 1. 核心目标

把硬编码的韩国旅行网页逐步升级为可长期使用的多旅行管理 App。用户和金鹿主要用手机/iPad，旅行中主要用手机；电脑端需要整洁，但不能成为设计优先级。

## 2. 不可偏离的体验要求

- iPhone 15 Pro、iPhone 16 Pro Max、iPad Safari 优先。
- 触控目标至少 44px；输入字体至少 16px，避免 iOS 自动放大。
- 适配刘海、灵动岛、底部手势条、横竖屏、动态字体和键盘弹起。
- 页面切换、卡片展开、原图查看、弹层、勾选、填写、删除、拖动都要有明确反馈且不掉帧。
- 待办和行李排序要接近 iOS 原生手感：长按进入拖动、占位稳定、自动滚屏、松手保存。
- 高频操作简单；低频修改和删除默认隐藏在“编辑”入口内，删除必须二次确认。
- 无网可以查看已缓存旅程和继续编辑，恢复网络后同步；不能让 API、天气或外部地图阻塞首屏。
- 中国大陆、韩国、美国都要有可用路径；地图提供 Apple Maps 主入口和 Naver/Kakao/Google/Amap/坐标备用入口。

## 3. 视觉要求

- 低饱和、浅色、有层次的手帐旅行风；可以使用绿色、雾蓝、浅紫、淡粉、暖黄、珊瑚色，不要所有页面只剩绿色。
- 卡片统一圆角、边框、阴影、间距和弹层规范；桌面端不能出现大面积空白、错位或不齐。
- 韩国画册直接展示已提供的手帐图片，比例不同也要用固定预览框 + `object-fit: contain` 保持整齐；原图页必须使用站内 viewer。
- 不使用满屏 emoji、AI 味提示语和“云端/本地”等技术状态干扰普通用户。
- 文件区只表示文件本身，不显示“待准备/已准备/已上传”等状态；待准备事项放在待办区。

## 4. 信息架构

固定 11 个 Tab：首页、行程、交通、地图、美食、随笔、待办、行李、文件、支出、锦囊。底部悬浮导航支持横向滑动和 hash 直达；首页是仪表盘，不依赖一页长滚动找内容。

## 5. 多旅程模型

```js
{
  id, name, brand, startAt, endAt, days, theme,
  members, timezone, cities, cover,
  itinerary, flights, hotels, gallery, maps, events, weather
}
```

- 所有动态记录必须带 `tripId`、稳定 `id`、`updatedAt`，不能把韩国数据写死到通用页面。
- 首次打开才选择旅程；选择后本机记住，离线进入上次旅程。
- 顶部提供低调切换入口，不要每次打开都强迫选择。
- 韩国是 9 天跨年深度旅行，绿色主题；香港是 12/18–12/20 周末旅行，紫色主题；厦门是 1/8–1/10 周末旅行，蓝色主题。
- 空白旅程必须显示友好空状态，不能混入韩国航班、酒店、照片或活动。

## 6. 各模块规则

- 行程：日期、时间、标题、地点、备注、地图、照片可编辑；桌面端为完整卡片，移动端为易展开时间线。
- 交通：航班展示航司、航班号、起降机场、日期时间、座位、机型、预订号、票号、行李规则和登机提醒；不显示用户删除的英文或免责声明文案。
- 酒店：中文为主、英文为辅，统一展示名称、地址、入住/退房时间等必要信息；不添加用户明确不要的早餐、人数、床型、房间数量或冗余句。
- 文件：名称、备注、附件、预览、下载；卡片顶格统一；站内打开提供返回文件、关闭、下载、新窗口打开。
- 支出：输入原币种，列表和统计统一人民币；总支出后显示“仅用于统计分析目的”；不做情侣分摊暗示。
- 美食、随笔、画册、地图原图：打开优先使用站内 viewer，并有返回路径；随笔支持每篇独立密码。
- 锦囊：韩语短语只保留常用短语，支持左右滑动；冬季贴士上下排列；补充韩国垃圾、厕所、扶梯、交通、餐厅、拍照、地铁座位、退税、出入境等注意事项。

## 7. 数据与同步

当前允许 localStorage/内置数据作为首屏兜底，但目标架构是 IndexedDB + 离线队列 + 单条 CRUD API + 幂等请求 + 版本号 + 冲突处理。不能长期依赖整批替换，否则 iPhone 和 iPad 同时修改会互相覆盖。

## 8. 手帐图片策略

不做网页内 AI 手帐转换。现有手帐图片作为静态素材直接展示；以后上传照片默认保留原图或做普通本地压缩，不引入 DeepSeek 等第三方模型和 API Key。

## 9. 发布前红线

必须完成静态语法、资源、线上入口、移动端、离线恢复、横竖屏、动态字体、双设备编辑和所有删除确认检查。没有真实设备或自动化验证时，不得宣称“全站无 bug”。
