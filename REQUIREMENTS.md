# 在璐上 需求文档

> 可乐 & 金鹿 · 韩国冬季之旅 · 2026.12.26 – 2027.1.3
> 参考项目：`~/Desktop/hanoi-vercel`（越南河内之旅，架构与规范以它为基准）

## 1. 背景与目标

- 在 `https://hankzhang.cloud/`（当前绑在河内项目上，韩国项目上线后可换绑）提供**完整的可视化界面**，可乐和金鹿把整个旅行计划都放在上面。
- **金鹿基本不用电脑**，主要用**手机和 iPad** 浏览。因此本项目的核心目标是：
  > **移动端（手机 + iPad）优先的浏览与阅读体验**，同时保留桌面端的完整观感。
- 支持 **Add to Home Screen**：添加到主屏幕后以 `standalone` 模式打开，像原生 App 一样全屏无浏览器边框。
- **跨地区可用性红线**：保证中国大陆 / 美国 / 韩国都能正常打开；所有图片、跳转、地图、天气、字体与脚本资源尽量采用同源或三地可稳定访问的路径。地图页不能依赖 Google/Naver/Kakao 等第三方 SDK 底图；网页内必须能独立读懂区域、路线、住宿与景点关系，采用固定比例手帐地图并支持拖动浏览 / 放大缩小；Apple Maps 作为 iPhone/iPad/Mac 的主跳转入口，Naver / Kakao / Google / Amap + 坐标作为备用。

## 2. 行程概要

| 日期 | 星期 | 城市 | 安排 |
|---|---|---|---|
| 12/25 晚 | 五 | 深圳 | 晚上出发去机场 |
| 12/26 凌晨 | 六 | 深圳 → 首尔 | 02:25 从深圳起飞，06:50 落地首尔后**立刻南下** |
| 12/26 – 12/29 | 六~二 | 釜山 | 海云台、胶囊小火车、甘川文化村、札嘎其海鲜市场 |
| 12/29 晚 | 二 | 釜山 → 首尔 | 晚一点再北上，KTX 傍晚出发，当晚入住首尔 |
| 12/30 – 1/3 | 三~日 | 首尔 | 景福宫、北村韩屋村、明洞、广藏市场、**普信阁跨年敲钟**、N首尔塔、弘大、圣水洞、汉江 |
| 1/3 晚 | 日 | 首尔 → 深圳 | 22:10 深夜航班回国 |
| 1/4 凌晨 | 一 | 深圳 | 凌晨落地到家 |

- **人员**：可乐 & 金鹿
- **时差**：韩国 KST = UTC+9，比中国快 1 小时（所有倒计时/当地时间按 +9 计算）
- 已确认航班：
  - 去程：Asiana OZ372，2026-12-26 02:25 深圳宝安 T3 → 06:50 首尔仁川 T2
  - 回程：Asiana OZ371，2027-01-03 22:10 首尔仁川 T2 → 2027-01-04 01:15 深圳宝安 T3
- 已确认釜山酒店：Asti Hotel Busan Station
  - 12/26–12/29，釜山站旁，方便 12/29 晚上直接坐 KTX 去首尔
- 已确认首尔酒店：Courtyard by Marriott Seoul Myeongdong
  - 入住：2026-12-29 15:00
  - 退房：2027-01-03 11:00
  - Confirmation：#93241473
- 现金与保险：不提前大量换韩元现金；到韩国优先用 Fidelity 卡在 ATM 无手续费取现，旅行保险（含医疗 / 航班延误）已确认不单独购买。

## 3. 功能需求

沿袭 hanoi 的全部功能模块，并做移动端适配。

### 3.1 信息架构：9 Tab 多视图 + 底部悬浮导航（v1.1 → v1.2）

> 用户反馈（v1.1）：*「之前越南的是一个网页一下子往下滑动那种，我觉得就不够直观，每次看最下面的都要从上往下滑动。这次最好在上面放那种 bar，比如交通、预订待办事项、行李清单、行程总支出、文件等等。」*
> 用户反馈（v1.2）：*「现在的地图、待办都很好用，但 AI 味太重、emoji 太多。结合参考设计稿做视觉优化。」*

v1.1 由「长页下滑」重构为 **多 Tab 视图**（类原生 App 结构）；v1.2 保留全部功能与结构，导航条移到**底部悬浮胶囊**（拇指可达），并做视觉去 AI 化：

- 底部悬浮胶囊导航（fixed + 毛玻璃 + 圆角 999px），共 9 个 Tab：**首页 / 行程 / 地图 / 交通 / 待办 / 行李 / 支出 / 文件 / 锦囊**。
- 每页只装一个主题的内容：不用找板块、不用长滚动，Tab 一键直达。
- 手机端导航横向可滑动，激活项自动滚动到可视中间。
- 支持分享链接直达：`/#itinerary`、`/#transport` 等 hash 路由（切 Tab 同步更新地址）。
- 首页作为「仪表盘」：水彩插画 Hero + 倒计时、下一站提醒、3 张快捷卡片（行程清单 / 备忘记录 / 我的收藏）、天气、三枚倒计时圆环。
- 记账悬浮按钮（FAB）只出现在「支出」页，避免全局悬浮干扰阅读。

### 3.2 模块清单

| 模块 | 功能 | 移动端适配要点 |
|---|---|---|
| 底部悬浮导航 | 9 个标签页视图切换（类原生 App 结构） | 悬浮胶囊 + 横向滑动 + hash 路由直达 |
| 首页仪表盘 | 水彩插画 Hero 轮播、倒计时、快捷卡片、天气、倒计时圆环 | 一屏概览，快捷卡片直达各 Tab |
| 天气 | **釜山 / 首尔双城市**，按行程阶段自动切换；当前 + 5 天预报 + 逐小时 | 城市 Tab、预报卡片横向滑动 |
| 地图 | 釜山 / 首尔两个视图切换，分类标记 | 固定比例手帐示意图；页面内可拖动浏览、可放大缩小；Apple Maps 主入口 + Naver/Kakao/Google/Amap/坐标备用 |
| 倒计时 | 三个关键日期圆环：出发、跨年、回程 | 三列并排 |
| 行程 | 9 天折叠卡片 + 时间线（今日自动展开）+ **日历条**（12月/1月行程日着色分类点） | 大触控目标、API 失败时用内置数据 + localStorage 兜底（弱网可看） |
| 交通 | 航班（去/回）、KTX、AREX、T-money、出租车、交通贴士 | 卡片化阅读，购票链接可点开 |
| 待办（预订） | 机票/签证/酒店/KTX 等预订待办 + 打卡清单 | 待办支持勾选、新增、编辑、删除、直接拖动排序；每行 ≥58px、进度条 |
| 行李清单 | 可编辑行李项 | 支持勾选、新增、编辑、删除、直接拖动排序；界面不显示“云端/本地”标签 |
| 支出 | 多币种（KRW/CNY/USD）→ 人均 RMB，预算进度 + **分类占比环形图**（交通/住宿/餐饮/门票/购物/其他） | **底部弹层表单 + 页内悬浮按钮**，手机单手可操作 |
| 文件 | 自定义文件卡片（名称 / 类型 / 状态 / 备注 / 压缩照片） | **云端持久化共享**；不预设具体证件或订单；图标由类型自动匹配，localStorage 仅作弱网兜底（隐私提示见 §3.4） |
| 锦囊 | 美食（釜山/首尔分组）、韩语短语、冬季贴士 | 折叠分组 + 横向滑动 |
| PWA | Add to Home Screen、离线壳 | manifest + 图标 + Service Worker |

### 3.3 视觉规范（v1.2 去 AI 化重设计）

> 用户反馈：*「ai 味特别重，特别是表情呀什么的 emoji 一看就是 ai」「地图、待办都很好用，保留功能结构，只做视觉」*。参考设计稿：`~/Downloads/IMG_3900 ~ 3904.PNG`。

设计要点（与参考稿高度一致）：

- **配色**：背景薄荷米白 `#F4F8F5`，白卡片大圆角（18px），薄荷绿主色 `#2FA36E`（渐变 `#4FBE93 → #2FA36E`），点缀浅蓝/浅粉/暖橙。
- **去 emoji**：全站 60+ 枚**手绘线条 SVG 图标**（stroke 1.7、圆头、随色）替换 emoji；数据里的 emoji 仅作图标索引键，界面只渲染线稿图标。
- **卡片与阴影**：白卡片 16–20px 圆角 + 柔和阴影 `0 2px 6px rgba(31,64,46,.05)`。
- **Hero**：4 张**水彩风 SVG 插画**（首尔塔 / 韩屋 / 釜山缆车 / 灯笼）5s 轮播 + 遮罩渐变，标题「韩国旅行计划」副题「遇见首尔的浪漫时光」。
- **底部悬浮胶囊导航**：白底毛玻璃、圆角 999px、激活项薄荷绿实心。
- **地图**：emoji 标记改为**分类色圆点 pill**（景点蓝 / 美食橙 / 交通灰 / 打卡点粉 / 住宿石板），图例随城市自动生成。
- **字体**：系统 sans-serif + `font-feature-settings` 数字等宽；标题加粗 700–800。
- 无渐变怪配色、无满屏 emoji、无 AI 腔文案。

### 3.4 文件区同步与隐私设计（v1.3）

文件区的状态、备注和压缩后的照片会写入 CloudBase `kr_docs` 集合，做到两个人、手机/iPad/电脑之间**云端持久化共享**。localStorage 只作为弱网/接口失败时的本机兜底缓存。

文件区不再预设「签证 / K-ETA / 机票行程单 / 酒店订单」等模板；前端只提供自定义文件卡片，用户自行填写文件名、选择文件类型、设置状态后再上传照片或备注。界面不提供 emoji/logo 选择，使用统一线条 SVG 图标按类型自动匹配。

隐私边界：护照、签证、保险单等敏感证件照片如果上传，就会进入共享云端数据库；建议优先只记录「已办理/已打印/电子版」和备注，确实需要两人同步查看时再上传压缩照片。

## 4. 非功能需求（移动端体验红线）

1. **触控目标 ≥ 44×44px**（iPhone HIG 标准），关键按钮 ≥ 48px。
2. **safe-area 适配**：`viewport-fit=cover` + `env(safe-area-inset-*)`，顶部 Tab Bar 不被刘海/灵动岛遮挡，记账弹层不被手势条遮挡。
3. **防 iOS 自动缩放**：输入框字体 ≥ 16px。
4. **横滑代替堆叠**：天气、贴士、短语等在手机上横向滑动（scroll-snap），减少纵向滚动距离。
5. **弱网兜底**：行程数据内置 + localStorage 缓存，API 挂了也有内容可看；天气等外部接口在部分地区受限时自动回落到本地兜底；SW 缓存静态资源。
6. **触屏地图防误触**：触屏设备上地图不拦截页面滚动。
7. **点击响应**：`touch-action: manipulation` 消除 300ms 延迟；不依赖 hover。
8. iPad（768–1024px）：Tab Bar 居中铺开、双列网格、更大间距（触屏优先）。

## 5. 域名规划

- 当前：先用 Vercel 默认域名 `korea-vercel.vercel.app`。
- 最终：换绑 `https://hankzhang.cloud/` 到本项目（河内项目改回 `hanoi-vercel.vercel.app`），命令见 README。
- 备选：`korea.hankzhang.cloud` 子域名（需在域名 DNS 服务商加 CNAME）。

## 6. 数据与更新方式

- 后端沿用 hanoi 规范：CloudBase HTTP 云函数（`korea-api`）+ NoSQL 集合（`kr_` 前缀，避免与河内项目数据冲突）。
- 行程 / 打卡 / 清单 / 记账的日常更新：CloudBase 控制台或 MCP 工具，不需要改代码。
- 汇率与预算：在 `index.html` 顶部常量区修改（当前 CNY 190 KRW / USD 6.80，预算 10000 RMB 占位）。

## 7. 待补内容（TODO）

- [x] 釜山酒店（Asti Hotel Busan Station，12/26–12/29）
- [ ] 真实照片（放入 `assets/photos/`，文件名对应 `PHOTO_LOCAL` 映射表）
- [ ] 预算总额确认
- [x] 文件区跨设备同步（状态 / 备注 / 压缩照片，见 §3.4）
- [ ] hankzhang.cloud 域名换绑（上线稳定后）
