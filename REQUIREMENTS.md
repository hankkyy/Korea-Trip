# 在璐上需求与产品红线

## 1. 核心目标

把一次性的旅行网页升级为可长期使用的多旅行管理 PWA。可乐和金鹿主要在 iPhone/iPad 使用，桌面端需要完整可靠，但不能以牺牲移动端为代价。

### 零号优先级：数据不能丢、不能乱、不能串

多设备、多用户的数据一致性和持久化高于新功能、视觉和发布速度。不能把“本机看得到”当作“已经保存”，不能因重试、乱序响应、刷新或切换设备产生重复记录，更不能静默覆盖另一人的编辑。

## 2. 体验和视觉要求

- iPhone 15 Pro、iPhone 16 Pro Max、iPad Safari 优先；触控目标至少 44px，输入字号至少 16px。
- 兼容 Safe Area、灵动岛、底部手势区、横竖屏、动态字体和键盘；页面、弹层、预览和底部导航不得溢出、遮挡或错位。
- 高频动作直接可用；编辑、删除、添加照片等低频或破坏性操作收进编辑态，删除必须二次确认。
- 待办和行李支持稳定的长按拖动、占位与自动滚动。
- 低饱和手帐旅行风，使用绿色、雾蓝、浅紫、淡粉、暖黄等层次；卡片、按钮、输入框、弹层统一圆角、边框、间距、图标和按钮高度。
- 不使用满屏 emoji、AI 式空话或向普通用户暴露“云端/本地/协议”等技术状态。

## 3. 信息架构与旅程隔离

固定 11 个页面：首页、行程、交通、地图、美食、随笔、待办、行李、文件、支出、锦囊。底部导航支持横向滑动和 hash 直达。

所有动态记录必须包含稳定 `id`、`tripId`、`updatedAt`，并在服务端根据认证身份授权。韩国、香港、厦门的资料、日期、城市、封面和动态数据相互隔离；空白旅程不得出现韩国的酒店、航班、图片或活动。

## 4. 权限、隐私与访客

- 可乐和金鹿为共同所有者。服务端必须从登录会话识别身份，绝不能相信客户端提交的用户名、UID 或权限字段。
- 访客只读公开总览行程；访客不能读取或枚举待办、行李、支出、文件、美食、随笔、灵感箱、旅程管理数据，也不能获得任何写入或私有文件临时地址。
- 支出和随笔逐条支持“双方可见”与“仅创建者可见”；仅创建者可见的数据不得随整表同步泄漏给另一位所有者。
- 证件、机票、酒店凭证与用户附件不得进入公开静态托管、Service Worker 公共缓存或公开仓库。私有文件必须使用 CloudBase 私有存储和短期签名地址。

## 5. 数据与同步要求

- 本机编辑先进入 IndexedDB 持久写入队列，再更新界面；云端确认前只能视为待同步，失败必须可见、可重试。
- 服务端写入必须原子、幂等，并保留可恢复历史。超时、响应丢失、断网、刷新、App 被系统关闭和重复请求都不能制造重复或丢失已确认数据。
- 不同记录并发编辑自动合并；同一记录冲突必须保留双方版本，不能采用“最后写入者胜出”静默覆盖。
- 发布验收至少覆盖两名用户、两台设备、多标签页、弱网/断网、提交后响应丢失、崩溃重开、删除传播、旅程切换、版本升级/回滚及全部动态数据类型。
- localStorage 与静态初始数据只能承担离线兜底和迁移，不得成为长期权威来源。

## 6. 模块规则

- 行程：日期、时间、标题、地点、备注、地图、照片可编辑；移动端使用易展开时间线。
- 交通和酒店：展示实际必要信息，中文为主、英文为辅；不添加用户不需要的早餐、人数、床型、房间数量或冗余免责声明。
- 文件：名称、备注、附件、站内预览、下载和新窗口打开；失败应展示可理解的原因和重试路径，不能空白或裸 404。
- 支出：原币种输入、人民币列表与统计；展示“仅用于统计分析目的”，不做情侣分摊暗示。
- 美食、随笔、画册、地图和文件优先站内 viewer，并始终提供返回和关闭。随笔支持独立密码。
- 灵感箱：仅需标题、可选地点、归类、来源链接和可选备注。小红书、抖音来源以外链打开；不抓取或完整搬运平台文章、图片、评论。卡片应简约、彩色、可读，整卡打开原帖，编辑保持次要。

## 7. 网络、离线与素材

- 中国大陆、韩国、美国均需要可用路径；地图提供 Apple Maps 主入口及 Naver/Kakao/Google/Amap/坐标备选。
- 无网至少可打开应用、查看缓存内容并保留本机编辑；网络恢复后补传，不允许天气、地图或外部服务阻塞首屏。
- 不做网页内 AI 手帐转换。手帐图片使用现有静态素材；以后上传照片可做普通本地压缩。

## 8. 当前文件上传限制与目标

当前历史内置凭证已在 CloudBase 私有存储，浏览器通过私有文件 ID 解析临时地址。用户手动上传的非图片附件仍随同步快照保存为 data URL，最大 1.5 MB，受 API 6 MB 请求体限制。这只是过渡实现。

下一步必须统一为：浏览器上传后只保存私有文件 ID；后端验证可乐/金鹿的成员权限后签发短期下载地址；两人均可打开共同文件，访客永远不能获取地址。不得通过把私有桶改成公开读取来绕过该问题。

## 9. 发布与验证红线

静态语法、资源、线上入口、离线恢复、横竖屏、动态字体、所有删除确认、两人双设备编辑和私有文件权限均需验证。没有真实设备或自动化覆盖时，不得宣称“全站无 bug”或“所有文件均可用”。
