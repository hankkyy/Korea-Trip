# 项目实际状态与迭代路线

更新时间：2026-09-10

## 零号优先级：多设备、多用户数据保障

数据一致性和持久化高于所有新功能、界面优化与发布进度。当前 protocol 2 已修复已知的重复写入、闪空、错误旅程覆盖、旧读取覆盖新编辑和响应丢失重试问题；可乐和金鹿是固定所有者，访客使用匿名会话时只能读取总览行程，未登录请求仍被拒绝；离线队列与版本会同时写入 localStorage 和 IndexedDB。服务端仍接收整份列表，但用稳定记录 ID 做三方合并：不同记录的并发修改自动合并，同一记录的真实冲突保留本机队列并提示备份。尚缺通用旅程成员模型、页面内冲突选择器、逐条变更协议、版本回滚界面和真实 iPhone/iPad 双设备验收。

金鹿反馈的历史美食收藏在当前云端、旧集合、迁移备份和现有历史快照中均未找到，丢失反馈属实；尚无法区分历史覆盖与当时未成功上传。恢复仍需检查她原设备浏览器中的缓存和待同步记录，在确认前不要清缓存或卸载网页 App。本轮另找到两个直接相关的界面问题：新建美食后没有立即进入编辑状态，以及异步加载文件数据后没有重绘美食/随笔；两处均已修复并加入重载回归。

## 最新修复：数据重复与同步闪空（优先于下方历史状态）

- 修复所有列表的先删后插：`cloudfunctions/korea-api/sync-store.js` 使用 `kr_sync_state` 不可变分块快照和事务切换当前版本。读取只返回完整快照；相同逻辑 ID 只保留最新记录，不按名称/金额合并真实记录。
- 原始集合不删除，首次迁移保留原始快照 `backupChunks`。待办原始 40 条对应 10 个逻辑 ID，新接口返回 10 条；支出 2 条、行李 16 条、行程 43 条、文件集合 7 条。打卡当前源集合返回 0 条，没有凭空填充或恢复推测数据。
- `assets/sync-client.js` 统一 HTTP 写入，取消直连 SDK 回退；补传单实例串行，并用 Web Locks 协调同源标签页。请求已发送但响应丢失时保留原请求 ID；仅确认对应请求，不删除随后产生的新修改。读取开始后若有编辑，丢弃该旧读取。
- 修正 POST 旅程 ID：后端读取请求正文中的 tripId，避免其他旅程写入韩国默认旅程。
- 正常云端空列表可以传播；网络失败保留当前内容；弹层打开和未同步修改期间不覆盖列表；数据没变化时不重复渲染。
- 旧离线队列和版本冲突保留本机。旅程菜单提供导出备份（包含 pendingWrites）和“备份后读取最新版本”，冲突修改另存本机归档。服务端按稳定 ID 做三方合并：不同记录自动合并，同一记录冲突不会静默覆盖；尚未提供页面内逐字段选择。
- 网关 `/korea-api` 原错误配置 `SCF` 已改为 `WEB_SCF`，保留路径去前缀与既有访问权限。`$DEFAULT` 原规则把所有请求送到版本 2；已清除旧匹配规则，指向 `$LATEST`。只上传函数不等于线上流量生效，后续发布必须核对此规则与实际响应的 `protocol: 2`。
- 后端 HTTP Nodejs18.15，超时 60 秒，请求体上限 6MB、单列表上限 5000 条，`kr_sync_state` 权限 PRIVATE，仅服务端访问。不可变快照保留最近 30 个版本和最近 100 个请求回执；Service Worker 当前为 v58。
- 身份与隐私：CloudBase 关闭匿名登录，启用用户名密码登录；函数权限和应用层都要求认证，应用层只允许可乐、金鹿两个 UID。证件、机票、酒店凭证及预览已迁到 CloudBase PRIVATE 存储；Vercel、CloudBase 静态包和 Service Worker 均排除 `assets/docs`。
- 自动验证：10/10 Node 回归通过；Chrome 移动尺寸浏览器覆盖 11 Tab、所有动态数据族、连续 9 次勾选、编辑、删除确认、离线补传、IndexedDB 恢复、11 个标签页基线、重载和加密随笔，pageerror 为 0。
- 生产验证：两个账号分别建立会话，对行程、待办、行李、打卡、支出、文件、旅行列表逐类完成 A 创建、B 读取编辑、A 再读取、删除与清理。最终基线为行程 43、待办 10、行李 16、打卡 0、支出 2、文件 7、旅行列表 0；所有逻辑 ID 唯一。非法旅程 ID 返回 400，未认证 API 返回 401。超 6MB 请求在网关 20 秒内未返回而由客户端中止，但测试前后版本完全一致、无测试记录写入。
- 未覆盖：真实 iPhone/iPad 及中韩运营商网络；CloudBase CLS 当前未开通，无法做集中日志检索；不可变历史块尚未增加自动垃圾回收，未来清理时必须保留当前快照和备份引用块。

> 本文件以当前源码为准。`已配置` 不等于 `用户可编辑`，`基础离线` 不等于 `完整离线同步`，`静态检查通过` 不等于 `真实设备无 bug`。

## A. 已完成

- 11 Tab 多视图和底部悬浮导航。
- 韩国、香港、厦门的旅程注册表、当前旅程记忆和基础选择入口。
- 韩国画册手帐图片、折叠展开、比例统一、站内原图 viewer。
- 首尔/釜山地图分类、固定比例图片地图、拖动缩放和站内原图 viewer。
- 文件站内预览层：返回、关闭、下载、新窗口打开；PDF、图片和部分附件有统一处理。
- 待办、行李、打卡、支出、文件、随笔、美食的部分增删改/勾选能力。
- 删除二次确认；待办、行李、文件、美食等低频删除/修改进入编辑模式。
- 待办和行李 pointer events 长按拖动、占位、requestAnimationFrame、边缘自动滚动。
- 航班座位、航司、预订号、票号、机型和中文飞行时长；酒店基础信息。
- 支出统一人民币显示和动态参考汇率失败兜底。
- 隐藏支出现在也会参与跨设备同步，不再只保存在单台设备。
- PWA standalone、Service Worker 缓存、localStorage 首屏兜底和 IndexedDB 持久离线写入队列。
- 美食新增后立即进入编辑并聚焦标题；文件异步加载结束后同步重绘文件、美食和随笔，避免刷新时收藏看似消失。
- 美食收藏移除重复的加号卡片，城市标题右侧“新增收藏”成为唯一入口并补齐点击绑定；编辑态使用统一等高按钮，完成、照片和删除分层显示。
- 敏感 PDF 与预览使用 CloudBase 私有文件 ID 和临时签名链接；公开静态入口直接访问旧 PDF 路径返回 404。
- 基础代码检查、资源检查和 `git diff --check` 已做过。
- 已为 `kr_` 动态集合增加 `tripId` 复合索引，并将 HTTP 函数和浏览器直连 fallback 改为按旅程查询，避免整集合扫描触发 `Read overrun`。
- 已将前端新版本发布到 Vercel `www.jinlu.cloud` 和 CloudBase 国内静态入口 `korea-hanoi-d4gj8vd2q1e7a3dc0.webapps.tcloudbase.com`；两边都已切换到当前 HTTP 网关地址。2026-09-10 最终 Vercel 部署为 `dpl_7LH6fg8anYBCnJVQh3N4JU3HcsQR`；CloudBase 通过静态托管直传同一 `/korea` 目录更新最终文件。两入口逐文件 SHA-256 与本地源码一致。
- 旅程选择层已支持当前旅程 JSON 导出、导入和本机恢复，导入有格式校验和二次确认。
- 打卡和自定义旅程均已纳入统一 protocol 2；自定义旅程使用 `kr_trips` 迁移源和 `kr_sync_state` 权威快照，并在启动时与本机定义合并。
- 历史版本曾修复匿名认证竞态；当前已关闭匿名登录并改为两个正式账号。底部导航保持稳定的居中横向布局，并在键盘或编辑弹层打开时隐藏。
- 已移除会在认证未稳定时返回空快照的 CloudBase 直连 `watch`，改为统一受认证 HTTP API 每 10 秒拉取；弹层或待同步修改存在时不会覆盖界面，离线队列每 15 秒及网络恢复时补传。
- 已将 `www.jinlu.cloud` 与 `jinlu.cloud` 加入 CloudBase Web 安全域名白名单，并重新发布 Vercel 生产版本。
- 已加固旅程隔离：HTTP API、同步快照、缓存回退均按 `tripId` 二次过滤；`kr_cache_custom_trips` 明确作为全局旅程目录，不与某个旅程的内容缓存混用。
- 修复 iOS Add to Home Screen 在美食/随笔页切换后底部导航视觉上移的问题：导航强制固定定位，并根据 `visualViewport` 动态同步底部偏移；Service Worker 已升级到 `lu-travel-v42` 并重新发布。
- 后端现仅保留统一 protocol 2 的列表读写入口；记录级三方合并以稳定 ID 和快照版本为依据，旧的单条直写路径不再作为前端同步通道。
- 2026-09-10 权限更新：`kele`（UID `2097823157655728129`）和 `jinlu`（UID `2097823165424365569`）是同等所有者。匿名访客只显示且只能读取总览行程；待办、行李、支出、文件、美食、随笔、灵感箱和旅程管理的读取均由函数返回 403，所有写入仍返回 403。支出和随笔逐条支持 `shared` 与 `private`：私人记录带创建者 `ownerId`，另一位所有者既不可读也不可修改或删除。函数、两套静态入口和 v53 Service Worker 均已发布；生产匿名会话实测行程 200，待办、支出、文件 403。
- 2026-09-11 灵感收集箱：`kr_inspirations` protocol 2 数据集只保存小红书分享链接、分享文案和自己的整理信息，不抓取或复制原帖。入口位于首页收集箱内，不再占用顶栏；复制链接后会先打开确认卡，自动猜测标题、归类和地点，支持改标题、归类、地点、备注和链接，再保存。已保存的每条灵感都可直接编辑或打开原帖。`manifest.json` 已声明 Android PWA `share_target`，安装 App 后可直接在小红书分享面板选择「在璐上」；iPhone Safari 不支持网页 App 接收系统分享，保留剪贴板入口。
- 密码变更记录：CloudBase 已接受两位所有者提供的新密码；两账号分别在正式站完成登录并被识别为所有者，页面无错误。密码明文不写入项目文档或仓库。
- 灵感箱集合 `kr_inspirations` 已在 CloudBase 创建并设为 PRIVATE；此前集合未创建导致的 500 已修复，正式所有者读取返回 200。
- 2026-09-11 全量发布：CloudBase 云函数 `korea-api` 已更新并处于 Active/Available；CloudBase 静态托管已上传新版 `/korea/index.html` 和 `/korea/sw.js`，其版本化页面地址已与本地 SHA-256 一致。Vercel 生产部署 `dpl_EvmNCmYN7FukHGwgLhE8xgFMcEER` 已绑定 `www.jinlu.cloud`。生产访客实测仅显示“行程”导航，行程 GET 200，待办、支出、文件 GET 403，页面错误为 0，Service Worker 已注册。
- 2026-09-10 全量界面审查：新增 `scripts/ui-audit.mjs`，在 390×844 和 1440×1000 下覆盖 11 个 Tab、支出新建/编辑、行程编辑、待办编辑、随笔编辑、旅程选择/设置、图片/文件预览和美食编辑，共 44 个状态。发现并修复行程弹窗“删除照片”横向溢出，以及旅程设置弹窗不能用 Escape 关闭；按钮文字/无障碍名称、横向溢出、容器溢出和页面错误均通过自动检查。美食编辑态的完成、添加照片、删除为统一等高的三列操作。发布后另修复访客模式的单一“行程”导航继承隐藏状态；访客也会注册静态 Service Worker，但不会启动写入补传或实时同步。
- 2026-09-11 美食回归：单个“新增收藏”是唯一入口；空状态不再生成第二个加号卡片。编辑态无附件时固定为保存、添加照片、删除三个等高按钮，查看照片只在附件存在且退出编辑后显示。浏览器回归新增三按钮与等高断言；缓存升级为 v54，避免旧 PWA 页面继续显示历史样式。
- 2026-09-11 美食低频操作：默认阅读态只渲染“编辑”（有附件时另加“查看照片”）；添加/替换照片与删除按钮只在编辑态生成。该规则不依赖 CSS 隐藏，避免旧样式或局部刷新误露出破坏性操作；Service Worker 升级至 v56。
- 2026-09-11 随笔编辑体验：标题和基本信息置顶，私密范围与密码组合为一张轻量安全卡，正文收敛到可编辑高度，照片改为紧凑的虚线收纳区；保存操作固定在弹层底部。随后已把灵感确认卡纳入手机与桌面界面审查，共 46 个页面与弹层状态通过；Service Worker 当前为 v58。

## B. 部分完成，不能当作完成

### 多旅程

香港（2026-12-18 至 12-20，迪士尼 + 市区）和厦门（2027-01-08 至 01-10，待定）已有 3 日基础模板和主题色，但仍缺少完整的独立酒店、交通、地图、照片、活动和资料内容。旅程设置已支持成员、城市、时区、封面和复制已有旅程框架；城市坐标和归档、无刷新切换仍待补充。

### 可编辑旅行资料

航班、酒店、首页画册、地图点位/路线、天气城市、跨年活动目前集中在 `TRIP_DATA` 或静态配置中，尚未全部提供页面新增、编辑、删除、排序和图片替换入口。

### 同步与离线

CloudBase 已按 `tripId` 隔离，动态列表统一经过 HTTP protocol 2；服务端使用不可变分块、事务切换、版本检查、幂等请求回执和三方合并，前端用 localStorage + IndexedDB 持久队列、同源标签页协调和冲突备份。公网函数要求登录，两套前端入口可访问。

它仍以整份列表快照提交，当前权限是固定两人白名单，而非通用成员模型；同一记录的并发修改需要导出备份后人工处理，没有页面内冲突选择器。最近 30 个版本已保留，但还没有版本浏览/回滚界面和定期恢复演练。因此不能宣称所有多设备场景已经完成。

### 兼容性

代码做过移动端 CSS 和部分性能优化；自动线上探测已通过，但还没有在真实 iPhone 15 Pro、iPhone 16 Pro Max、iPad Safari 上逐页验收，也没有完成中国大陆/韩国不同运营商的断网恢复、横竖屏、动态字体、键盘和后台恢复的完整测试。

## C. 尚未实现

- 航班、酒店、活动、地图、画册、天气的完整资料管理 UI。
- 旅程成员、时区、封面、城市、复制、归档、排序和完整删除。
- 单条/逐字段同步、页面内冲突解决器和通用旅程成员权限。
- 版本浏览/回滚和跨设备备份合并；JSON 导入/导出、基础本机恢复和 30 版服务端历史已完成。
- 集中错误监控、真机验收矩阵和自动发布流水线；当前已有同步单测、浏览器回归和线上 smoke check。

## D. 迭代顺序

1. **P0 数据持久化**：在现有认证、IndexedDB、幂等和 30 版历史上，补通用旅程成员权限、版本浏览/回滚和定期恢复演练。
2. **P0 多用户一致性**：把快照协议升级为逐条变更并增加页面内同记录冲突处理；完成真实双设备、弱网、升级与回滚验收。
3. **P0 全数据类型回归**：自动化已覆盖待办、行李、打卡、支出、美食、随笔、文件、行程和旅行列表；继续查找金鹿原设备中的历史美食缓存，并补真机验证。
4. **功能开发**：P0 达标后再补旅程设置与航班、酒店、活动、地图、画册、天气的资料 CRUD。
5. **体验与发布保障**：真实地区/设备验收、错误监控和视觉完善；新功能必须带对应的数据一致性测试。

## E. 当前关键代码位置

- `index.html`：页面、样式、旅程配置、交互和本地缓存。
- `cloudfunctions/korea-api/index.js`、`sync-store.js`：CloudBase HTTP API 和 protocol 2 快照存储，禁止恢复旧的先删后插逻辑。
- `manifest.json`：App 名称“在璐上”。
- `sw.js`：离线缓存，本轮缓存版本 `lu-travel-v53`，敏感文件不得加入缓存。
- `assets/photos/`：画册和旅行图片。
- `assets/maps/`：釜山、首尔手帐地图。
- `scripts/verify.mjs`、`sync-test.mjs`、`sync-browser.mjs`、`ui-audit.mjs`：线上 smoke、同步单元回归、浏览器交互回归和跨尺寸界面审查。

## F. 文档维护规则

每次代码变更必须同步：完成项、未完成项、数据结构变化、部署入口、缓存版本、已知风险和验证结果。不得只写“已优化”，必须写清楚验证范围和未覆盖范围。
