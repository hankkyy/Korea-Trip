# 项目实际状态与迭代路线

更新时间：2026-09-05

> 本文件以当前源码为准。`已配置` 不等于 `用户可编辑`，`基础离线` 不等于 `完整离线同步`，`静态检查通过` 不等于 `真实设备无 bug`。

## A. 已完成

- 11 Tab 多视图和底部悬浮导航。
- 韩国、香港、厦门的旅程注册表、当前旅程记忆和基础选择入口。
- 韩国画册手帐图片、折叠展开、比例统一、站内原图 viewer。
- 首尔/釜山地图分类、固定比例图片地图、拖动缩放和站内原图 viewer。
- 文件站内预览层：返回、关闭、下载、新窗口打开；PDF、图片和部分附件有统一处理。
- 待办、行李、打卡、支出、文件、随笔、美食的部分增删改/勾选能力。
- 删除二次确认；待办、行李、文件、美食等低频删除/修改进入编辑模式。
- 待办和行李 pointer events 长按拖动、占位、requestAnimationFrame、边缘自动滚动。
- 航班座位、航司、预订号、票号、机型和中文飞行时长；酒店基础信息。
- 支出统一人民币显示和动态参考汇率失败兜底。
- 基础 PWA standalone、Service Worker 缓存、localStorage 首屏兜底和离线写入队列。
- 基础代码检查、资源检查和 `git diff --check` 已做过。
- 新增 `scripts/verify.mjs`：当前从开发环境验证 3 个线上入口、32 个缓存资源、4 个 API 和天气接口均通过。
- 旅程选择层已支持当前旅程 JSON 导出、导入和本机恢复，导入有格式校验和二次确认。
- 后端已增加按 `tripId` 隔离的单条更新/删除接口，并支持 `baseUpdatedAt` 乐观冲突检测；前端尚未全部迁移使用。

## B. 部分完成，不能当作完成

### 多旅程

香港（2026-12-18 至 12-20，迪士尼 + 市区）和厦门（2027-01-08 至 01-10，待定）已有 3 日基础模板和主题色，但仍缺少完整的独立酒店、交通、地图、照片、活动和资料内容。旅程设置已支持成员、城市、时区、封面和复制已有旅程框架；城市坐标和归档、无刷新切换仍待补充。

### 可编辑旅行资料

航班、酒店、首页画册、地图点位/路线、天气城市、跨年活动目前集中在 `TRIP_DATA` 或静态配置中，尚未全部提供页面新增、编辑、删除、排序和图片替换入口。

### 同步与离线

CloudBase 已按 `tripId` 隔离，已有本地缓存和基础重试队列；后端已有单条更新/删除和基础 `baseUpdatedAt` 冲突检测，但多数前端保存仍是整批替换，缺少单条新增接入、幂等 ID、完整版本合并和同步状态恢复。因此双设备同时修改仍可能后保存覆盖先保存。

### 兼容性

代码做过移动端 CSS 和部分性能优化；自动线上探测已通过，但还没有在真实 iPhone 15 Pro、iPhone 16 Pro Max、iPad Safari 上逐页验收，也没有完成中国大陆/韩国不同运营商的断网恢复、横竖屏、动态字体、键盘和后台恢复的完整测试。

## C. 尚未实现

- 航班、酒店、活动、地图、画册、天气的完整资料管理 UI。
- 旅程成员、时区、封面、城市、复制、归档、排序和完整删除。
- IndexedDB 正式数据层、单条同步、冲突解决和多设备合并。
- 版本回滚和跨设备备份合并；JSON 导入/导出、基础本机恢复已完成。
- 自动化 E2E 测试、错误监控、真机验收矩阵和发布回归脚本。
- 当前 `scripts/verify.mjs` 是线上 smoke check，不是替代真实浏览器点击的完整 E2E 测试。

## D. 迭代顺序

1. **V1.4 旅程设置**：补齐成员、时区、城市、封面、空状态和无刷新切换。
2. **V1.5 资料 CRUD**：航班、酒店、活动、地图、画册、天气全部页面化。
3. **V1.6 数据可靠性**：IndexedDB、单条 API、离线队列、幂等、版本和冲突处理。
4. **V1.7 质量保障**：真机、E2E、错误监控和网络环境验收。

## E. 当前关键代码位置

- `index.html`：页面、样式、旅程配置、交互和本地缓存。
- `cloudfunctions/korea-api/index.js`：CloudBase HTTP API，目前包含批量替换逻辑。
- `manifest.json`：App 名称“在璐上”。
- `sw.js`：离线缓存，当前缓存版本 `korea-trip-v23`。
- `assets/photos/`：画册和旅行图片。
- `assets/maps/`：釜山、首尔手帐地图。
- `scripts/verify.mjs`：发布前线上入口、资源、API 和天气 smoke check。

## F. 文档维护规则

每次代码变更必须同步：完成项、未完成项、数据结构变化、部署入口、缓存版本、已知风险和验证结果。不得只写“已优化”，必须写清楚验证范围和未覆盖范围。
