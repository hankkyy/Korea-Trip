# 项目实际状态与迭代路线

更新时间：2026-09-12
实现基线：`dad2bff`（零技术门槛体验优化）；前端缓存版本：`lu-travel-v86`

本文件记录可验证的当前状态。`已发布` 不等于所有设备均已更新，`自动化通过` 不等于真实设备无 bug，`本机可见` 不等于云端已确认。

## 已完成并已有证据

### 数据与访问控制

- protocol 2 使用不可变分块快照、事务版本、幂等请求回执、最近版本历史和按稳定 ID 的三方合并，修复过的重复、闪空、乱序读取覆盖和跨旅程写入问题不再走旧“先删后插”通道。
- 写入队列同时使用 IndexedDB 与 localStorage 兜底；启动、前台恢复和定时同步会拉取远端，网络失败不应以空数据覆盖当前界面。
- 修复了未显示远端快照却推进版本、保存响应期间后续编辑抹掉远端新增记录、多标签页离线队列互相覆盖、存储失败仍显示成功、旧缓存复活已删除旅程等时序问题。
- 全局待同步提示覆盖所有动态模块，关闭页面时若存在未同步修改会提醒。
- 网站彻底取消账号、登录和身份验证；所有访问者直接读取、添加、修改和删除同一份共享旅程数据。
- 历史标为私有的支出、随笔和文件记录也向网站访问者显示；未来写入统一保存为共享记录。

### 界面和产品流程

- 11 页导航、旅程选择、行程/交通/地图/画册基础内容、待办/行李拖动、美食低频编辑模式、随笔编辑层、文件 viewer、支出统计均已存在。
- 灵感箱已简化为“新建灵感”，只保留标题、可选地点、归类、来源链接、可选备注。卡片使用稳定 ID 的彩色主题，卡片主体打开小红书或抖音来源，编辑为次要动作。
- 手机端灵感卡片单列、限制最大宽度并裁切装饰溢出；CloudBase 根入口和 `/korea/` 镜像各自注册 Service Worker scope，避免旧页面壳串用。
- 前端不再加载 CloudBase 登录 SDK，也不创建匿名会话；所有同步和文件请求都不携带 Authorization 头。

### 本轮 UI 精修

- 依据自然手账感设计规格复核 11 个页面、所有编辑层和预览层；灵感区改为桌面横向叙事布局，卡片内容自适应，不显示来源平台标签。
- 手机首页主视觉收紧；窄屏美食编辑按钮改为主操作独占一行；行程编辑层加入移动端粘性保存区；顶部旅程切换、关闭、编辑、筛选、图片缩放等小控件统一提升触控尺寸。
- 跨年企划和旅程选择层改用统一 SVG 图标；UI 审计增加字段最小高度、按钮文字裁切、同组控件重叠和整页横向溢出检查。
- 面向完全不懂技术的使用者重写保存、文件和同步文案；界面只显示“保存中/已保存”，网络恢复、冲突合并和重试均由系统处理。
- “从手机添加”会直接打开文件选择器，照片自动缩小、文件自动命名和上传，不再先创建空白卡片；PDF 预览不再用直链触发自动下载，并加入加载状态和自动重试。
- 支出和随笔移除无意义的单选“可见范围”；新随笔默认直接保存，密码改为主动需要时才设置；新建旅程只保留名称、日期、同行人和城市。
- 编辑弹层不再因误点背景而关闭，降低尚未保存内容丢失的风险。

### 文件修复

- 已核验历史内置 PDF 对象存在。新附件上传到 `private/shared/<tripId>/`，记录只保存文件 ID；公共后端按旅程路径签发 15 分钟临时地址。
- Storage ACL 已现场核验为 `PRIVATE`；原来被 Git 跟踪的 30 个私人凭证和预览文件已取消跟踪并加入发布忽略，本机文件保留。

## 已执行的验证

| 项目 | 覆盖内容 | 结论/边界 |
|---|---|---|
| `node scripts/sync-test.mjs` | protocol 2、连续编辑、跨标签队列、持久化失败、公开共享规则和文件范围 | 16 项通过；不是真实云端或网络测试 |
| `node scripts/sync-browser.mjs` | 无凭证访问、多动态数据族、离线/重载、一键文件添加、PDF 不自动下载、可选随笔密码 | 已通过；临时文件 URL 使用 mock |
| `node scripts/ui-audit.mjs` | 320/390 手机、768 平板、1440 桌面的 11 页、卡片、编辑层与 viewer；触控尺寸、裁切、重叠、横向溢出 | 96 个状态通过；不能替代真实 Safari 真机 |
| `node scripts/verify.mjs` | 线上入口、资源、无凭证公开 API 和文件范围校验 | 用于发布后 smoke；8 个同步 API 必须直接返回成功 |
| 静态检查 | Node 语法与 `git diff --check` | 每次改动后必须重新执行 |

## 2026-09-12 生产发布

- CloudBase `korea-api` 已更新并确认状态 `Available`、代码结果 `success`。
- CloudBase 共享根入口与 `/korea/` 已上传 `lu-travel-v86`；独立 webapps 已发布 `korea-016`（SUCCESS），使用精简发布包避免原始照片打包超时。
- Vercel 生产部署 `dpl_Yzp7hzfSBcicDoQVVeEZy1oLcTUw` 已就绪并绑定 `https://www.jinlu.cloud/`。
- 三个生产域名的 `index.html`、`sw.js`、`assets/sync-client.js` 与本地发布候选 SHA-256 完全一致。
- CloudBase 函数调用规则已现场核验为 `{"*":{"invoke":true}}`；不带身份头的 8 个同步 API 读取和一次内容不变的待办写入均成功。
- 线上页面不包含登录/退出入口；5 个入口与 8 个同步 API smoke 全部通过。

## 仍未完成或未充分验证

### P0：共同文件真实双设备验收

代码和 Storage 非公开 ACL 已完成；仍需要两台真实设备分别上传、预览、下载同一文件，确认公共云函数签发的临时链接跨设备可用。

### P0：真实多设备一致性

尚未完成两人、两台真实 iPhone/iPad 或桌面设备的弱网、断网、响应丢失、冲突、后台恢复、更新旧 Service Worker、清缓存前待同步提示等完整矩阵。同一记录冲突现按更新时间自动选择最新版本，旧版本队列自动重建同步基线并重试。

### 功能完整性

- 香港和厦门已有独立行程与素材，但航班、酒店、活动、地图、画册、天气仍有大量 `TRIP_DATA` 静态配置，未全部提供资料 CRUD。
- 没有版本浏览/回滚界面、定期恢复演练、集中错误监控或自动发布流水线。

## 后续优先级

1. 先完成共同文件双设备真实验证。
2. 再完成真实双设备弱网/冲突/升级回滚验收和页面内冲突处理。
3. 然后补全香港、厦门和各旅行资料的可编辑管理。
4. 每次功能变更必须带同步回归、移动端 UI 审查、Vercel 与 CloudBase 双入口发布验证，并更新本文件。

## 历史问题的结论

金鹿反馈的历史美食收藏在当时检查的云端、旧集合、迁移备份和现存历史快照中未找到，丢失反馈属实。除非她原设备仍保留旧浏览器缓存或待同步记录，否则无法从当前云端推断恢复内容。不要清除她原设备的网站数据或卸载 PWA，直到完成该设备的恢复检查。
