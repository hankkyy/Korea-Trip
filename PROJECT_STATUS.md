# 项目实际状态与迭代路线

更新时间：2026-09-11
源码基线：`6a5e476`；前端缓存版本：`lu-travel-v77`

本文件记录可验证的当前状态。`已发布` 不等于所有设备均已更新，`自动化通过` 不等于真实设备无 bug，`本机可见` 不等于云端已确认。

## 已完成并已有证据

### 数据与访问控制

- protocol 2 使用不可变分块快照、事务版本、幂等请求回执、最近版本历史和按稳定 ID 的三方合并，修复过的重复、闪空、乱序读取覆盖和跨旅程写入问题不再走旧“先删后插”通道。
- 写入队列同时使用 IndexedDB 与 localStorage 兜底；启动、前台恢复和定时同步会拉取远端，网络失败不应以空数据覆盖当前界面。
- 两名固定共同所有者由服务端认证令牌校验；访客只允许读取总览行程，敏感数据读取和一切写入拒绝。
- 支出和随笔的创建者私有记录由服务端过滤，另一位所有者不能读取或改写。

### 界面和产品流程

- 11 页导航、旅程选择、行程/交通/地图/画册基础内容、待办/行李拖动、美食低频编辑模式、随笔编辑层、文件 viewer、支出统计均已存在。
- 灵感箱已简化为“新建灵感”，只保留标题、可选地点、归类、来源链接、可选备注。卡片使用稳定 ID 的彩色主题，卡片主体打开小红书或抖音来源，编辑为次要动作。
- 手机端灵感卡片单列、限制最大宽度并裁切装饰溢出；CloudBase 根入口和 `/korea/` 镜像各自注册 Service Worker scope，避免旧页面壳串用。
- iPhone 登录 SDK 已本地随站点发布，避免依赖外部 SDK 域名导致登录按钮不可点击；密码输入支持显示/隐藏。

### 文件修复

- 已核验已知历史内置 PDF 私有对象存在。旧同步记录中指向 `/assets/docs/` 的 7 个已知文件名，会改写为 CloudBase 私有文件 ID，再解析临时地址用于站内预览。
- 浏览器回归覆盖旧 URL 迁移、临时 URL 调用和 PDF iframe 预览，避免重新公开敏感凭证。

## 已执行的验证

| 项目 | 覆盖内容 | 结论/边界 |
|---|---|---|
| `node scripts/sync-test.mjs` | protocol 2 存储、合并、幂等、版本行为 | 已通过；不是真实云端或网络测试 |
| `node scripts/sync-browser.mjs` | 多动态数据族、离线/重载、访客、灵感、私有 URL 迁移 | 已通过；CloudBase 临时 URL 使用 mock |
| `node scripts/ui-audit.mjs` | 手机/桌面页面、卡片、编辑层与 viewer 的布局检查 | 已通过最近一轮审查；不能替代真实 Safari 真机 |
| `node scripts/verify.mjs` | 线上入口、资源、受保护 API 预期状态 | 用于发布后 smoke；无测试 token 时受保护 API 的 401 是预期 |
| 静态检查 | Node 语法与 `git diff --check` | 每次改动后必须重新执行 |

## 仍未完成或未充分验证

### P0：共同私有文件的统一存储流程

目前历史内置 PDF 使用私有文件 ID，但手动上传附件仍是 data URL 并受 1.5 MB 限制；两者不是同一条可审计的权限链路。下一项开发必须实现服务端签发的共同所有者临时 URL 和只保存私有文件 ID 的上传记录，然后使用可乐、金鹿两套真实会话分别验证预览、下载、拒绝访客和失败回滚。

### P0：真实多设备一致性

尚未完成两人、两台真实 iPhone/iPad 或桌面设备的弱网、断网、响应丢失、冲突、后台恢复、更新旧 Service Worker、清缓存前待同步提示等完整矩阵。当前同一记录冲突仍通过保留本机版本和备份处理，没有页面内选择器。

### 功能完整性

- 香港、厦门仍是基础模板，缺完整独立资料。
- 航班、酒店、活动、地图、画册、天气仍有大量 `TRIP_DATA` 静态配置，未全部提供资料 CRUD。
- 没有通用旅程成员模型、版本浏览/回滚界面、定期恢复演练、集中错误监控或自动发布流水线。

## 后续优先级

1. 先完成共同私有文件上传、服务端授权临时 URL、迁移与双所有者真实验证。
2. 再完成真实双设备弱网/冲突/升级回滚验收和页面内冲突处理。
3. 然后补全香港、厦门和各旅行资料的可编辑管理。
4. 每次功能变更必须带同步回归、移动端 UI 审查、Vercel 与 CloudBase 双入口发布验证，并更新本文件。

## 历史问题的结论

金鹿反馈的历史美食收藏在当时检查的云端、旧集合、迁移备份和现存历史快照中未找到，丢失反馈属实。除非她原设备仍保留旧浏览器缓存或待同步记录，否则无法从当前云端推断恢复内容。不要清除她原设备的网站数据或卸载 PWA，直到完成该设备的恢复检查。
