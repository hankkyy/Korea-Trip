# 在璐上全量上线审计

审计日期：2026-09-11

## 审计范围与结论

审计覆盖 11 个页面、所有编辑层和预览层、韩国/香港/厦门旅程隔离、认证和访客权限、全部动态数据族、离线队列、多标签页、多设备合并、私有文件、Service Worker、Vercel 与 CloudBase 发布配置。

本次修复后，自动化范围内没有阻断上线的问题。仍有两类只能由真实使用者完成的验收：两位所有者各自账号打开同一个私有文件；两台真实 iPhone/iPad 在弱网、后台恢复与 PWA 升级中的共同编辑。它们列在本文末尾，不能用模拟测试替代。

## 本次发现并修复

| 领域 | 原风险 | 当前处理 |
|---|---|---|
| 同步基线 | 远端数据未显示时也可能推进版本 | 仅在快照真正应用到界面后推进 |
| 连续编辑 | 前一次保存响应可能覆盖响应期间的新编辑或远端新增 | 按稳定 ID 重放本次差量并保留远端新增 |
| 多标签页 | 本机内存缓存可能看不到另一标签页的队列 | 每次读取优先读取共享 localStorage，IndexedDB 继续做恢复副本 |
| 本机持久化 | localStorage 与 IndexedDB 同时失败仍可能显示已保存 | 保存立即失败并持续提示，禁止误报已同步 |
| 轮询体验 | 输入框编辑中 4 秒刷新可能重绘表单 | 输入聚焦或弹层打开时暂缓应用远端快照 |
| 文件 | data URL 塞入同步快照，体积大且权限链路不统一 | PRIVATE Storage + 后端成员/旅程授权 + 15 分钟临时 URL |
| 缓存隐私 | Service Worker 可能缓存私有临时地址 | 只缓存同源 App 壳和公开静态资源 |
| 私密资料 | 30 个凭证与预览文件曾被 Git 跟踪 | 取消跟踪并加入 Git/Vercel 忽略，本机原文件保留 |
| 旅程删除 | 旧设备本地缓存可能把已删除旅程重新上传 | 云端/待同步队列为准，旧本地版本只留恢复备份 |

### 2026-09-12 UI 复核

| 领域 | 发现 | 当前处理 |
|---|---|---|
| 信息密度 | 桌面灵感区只有一张卡时横向留白过大 | 改为说明栏 + 内容栏；卡片按内容收紧 |
| 移动首屏 | 390px 首页主视觉占据首屏过多 | 使用 330–390px 的内容优先高度范围 |
| 窄屏编辑 | 320px 美食编辑的三列操作按钮过挤 | “完成”独占一行，照片/删除并列 |
| 长表单 | 行程编辑保存操作需要滚动到底部 | 移动端加入粘性保存区，字段仍可独立滚动 |
| 触控与图标 | 旅程切换、编辑、关闭、缩放等小控件偏小；部分静态图标使用 emoji | 统一提升到 40–50px 触控高度，静态图标改为 SVG |
| 自动审计 | 原检查未覆盖字段高度、裁切、控件重叠 | `ui-audit.mjs` 新增四类几何检查，四档尺寸 96 状态通过 |

## 验证证据

- 同步和权限单测：16 项通过。
- 浏览器全流程：11 页、所有动态数据族、快速切换、离线 IndexedDB 恢复、轮询、重载、访客边界通过。
- UI：320×667、390×844、768×1024、1440×1000 共 96 个页面和弹层状态通过，无检测到的横向溢出、空按钮或未关闭弹层。
- 静态资源：页面引用和 Service Worker 预缓存资源存在；私有目录已排除于静态发布。
- CloudBase：环境状态 NORMAL，NoSQL 和 Storage 正常；`korea-api` 为 Nodejs18.15 HTTP 函数；函数入口要求已认证调用；Storage ACL 为 PRIVATE。

## 数据保存承诺的准确边界

- 已由服务端确认的数据以 `kr_sync_state` 不可变分块快照为权威，每数据族保留最近 30 个版本指针和最近 100 个请求回执。
- 未确认编辑同时写入 localStorage 和 IndexedDB 队列；网络恢复后幂等重试。页面会明确显示待同步，不能把它理解为云端已完成。
- 不同记录的并发修改自动三方合并。同一记录双方同时修改会阻塞本机队列并提示导出备份，不会静默覆盖。
- 浏览器网站数据被用户或系统彻底清除时，本机未同步数据无法凭空恢复；因此存在待同步提示时不要卸载 PWA 或清除网站数据。

## 发布后人工验收

1. 可乐上传一个新 PDF，金鹿在另一台设备预览、下载；再反向测试一次。
2. 访客确认看不到文件、美食、待办、行李、支出、随笔和灵感。
3. 两台设备分别新增不同待办与美食，4 秒内互相出现；同时修改同一条，确认双方版本不被覆盖并能导出本机备份。
4. 一台设备断网新增待办，完全关闭再打开 PWA，确认待同步提示和内容仍在；联网后另一台设备可见。
5. iPhone/iPad 更新到 `lu-travel-v82`，确认首页、底部导航、键盘弹出、文件 viewer 和横竖屏。

## 发布记录

2026-09-12 已发布 CloudBase HTTP 函数、CloudBase 共享根入口与 `/korea/`、CloudBase 独立 `webapps` 镜像，以及 Vercel `www.jinlu.cloud`。文件预览修复已随 `lu-travel-v81` 发布：Vercel 部署 `dpl_HsqfsgSPGZYZaXGTVmsXdbEDmJpF`，CloudBase webapps 版本 `korea-008`（SUCCESS），Hosting 四个入口文件上传成功。
